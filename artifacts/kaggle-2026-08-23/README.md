# Curated Kaggle evidence — 2026-08-23

These small files were selected from:

```text
/media/waqasm86/External1/Waqas-Projects/Project-vLLM-Kaggle/Kaggle-Session-Files-3/
```

The source collection remains the authority. The wheel (383,922,260 bytes),
Qwen archive (4,773,220,584 bytes), full logs, notebook, and extracted weights
are intentionally absent from Git.

## Provenance

- `environment.txt` is a concise transcription of
  `kaggle-build-environment.txt` and the functional validation fingerprint.
- `source-identity.txt`, `nccl-smoke.log`, `native-imports.log`,
  `runtime-validation.log`, the Qwen manifest, and API JSON files are copied
  verbatim from the source evidence.
- `qwen-sharded-validation.log` is a labeled selection of verbatim PASS/backend/
  endpoint lines from the save, reload, and server logs.
- `SHA256SUMS.txt` records independently recomputed large-artifact digests.

The logs demonstrate functional behavior only on the documented Kaggle
environment. Generated text is evidence that inference completed, not an
evaluation of model answer quality.
